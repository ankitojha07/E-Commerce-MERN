{
  "version": 2,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/auth/(.*)", "dest": "dist/app.ts" },
    { "src": "/(.*)", "dest": "dist/app.ts" }
  ]
}
